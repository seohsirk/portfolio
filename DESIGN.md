# nnm.im — 서한석 포트폴리오 디자인 시스템

> 이 문서가 디자인의 단일 진실 원천이다. 토큰 값은 `app/globals.css` 가 실물이고,
> 여기에는 **왜 그렇게 정했는지**를 적는다. 둘이 어긋나면 globals.css 가 맞다.

## 개요

| | |
|---|---|
| 무엇 | AI Technical Product Manager 서한석의 비공개 포트폴리오 |
| 누가 읽나 | 접근 코드를 받은 채용 담당자 |
| 3초에 느껴야 할 것 | **「직접 만드는 사람이네」** |
| 경쟁 상대 | Squarespace · Wix · Webflow · Notion 으로 만든 PM 포트폴리오 |
| 그래서 | 화려함이 아니라 **판면 장악력**으로 증명한다. 빌더가 못 내는 것은 애니메이션이 아니라 41em 판면·1px 레일·모노 비율이다 |

## 구조 — 질문이 본문이다

```
물어야 했던 것들                      ← 아래 넷이 원래 물었어야 할 질문임을 말한다
01  질문                             ← 클릭하면 상세
    → 답 한 줄 (설명이 아니라 판단)
02 · 03 · 04
이 질문들을 만난 곳                   ← 경력
쓰는 것 · 그 밖에                     ← 전부 같은 표 꼴
```

⛔ **「소개 → 케이스 → 경력」으로 되돌리지 말 것.** PM 포트폴리오 교과서 공식이고,
   2026-09-22 에 그 구조로 만들었다가 「어색하고 뻔하다」로 폐기했다.
⛔ **산문 문단을 첫 화면에 두지 말 것.** 질문·답 한 줄·표만 쓴다.

## 색 — 판정을 색이 아니라 밀도로 말한다

```
                Light       Dark
paper           #f7f7f5     #101113      순백·순검정 둘 다 금지
paper-chosen    #ffffff     #17191c      채택된 갈래만 한 단 밝게
ink             #191a1c     #e8e4db
ink-muted       #6b6a66     #8e8b84      기각된 옵션 본문
ink-faint       #9c9a93     #63615c      라벨·메타
rule            #dfdbd1     #2a2d31
rule-strong     #c9c4b8     #454a50
accent          #c2452d     #e4674a      교정 잉크 주홍
body-weight     400         450          다크에서 한 단 올린다
```

- **accent 는 판정에만 쓴다.** 기각·채택 한 줄, 답의 화살표, 링크. 그 밖엔 쓰지 않는다.
- ⛔ 크림색 배경 금지 — 크림+세리프+테라코타는 AI 시안 1번 클리셰다. 중성 회백을 쓴다.
- ⛔ 다크에 글로우·컬러 할로·그림자 금지.
- 다크에서 본문 굵기를 올리지 않으면 다크가 「읽기 싫은 쪽」이 된다.

## 타이포그래피

```
한국어 본문   Pretendard Variable      17px / 1.85 / keep-all     ← 92%
영문·숫자     JetBrains Mono           11~15px                     ← 8% 상한
```

- 사용자가 Pretendard 로 지정했다. 한국 웹의 사실상 표준이라 **한글로는 차별화가 안 난다.**
  대신 회사명·기술명·수치가 전부 영문이므로 **모노에서 개성을 만든다.**
- 🔴 **모노 8% 는 상한이 아니라 방어선이다.** 넘기면 「개발자 코스프레」가 된다 —
  터미널 흉내·초록 글씨·타이핑 효과. 실제 엔지니어가 3초에 알아본다.
- 스케일을 벌린다. 라벨 11px ↔ 첫 제목 4.25rem = 약 6배.
  2026-09-22 에 11px~1.75rem(한 뼘) 으로 만들었다가 「밋밋하다」로 폐기했다.
- 한국어 조판 — `word-break: keep-all` · `text-wrap: balance`(제목) · 본문 폭 40em.
  ⛔ `ch` 단위를 쓰지 말 것. 한글에선 거짓말이다.

## 어형 — 자리마다 다르다

```
질문              「~까?」 로 통일. 자문하는 톤
답 한 줄          평서체 「~다」. 설명이 아니라 판단
케이스 본문       합니다체 서술형. 마침표를 찍는다
「만든 것」 절     개조식(명사형 종결). 마침표 없음
라벨 · 태그       명사구
```

⭐ **판정법 — 그 자리에 마침표를 찍나.** 찍으면 서술형, 안 찍으면 개조식.
   한국 제안서·보고서의 표준은 개조식이다. 목록·표·라벨에 서술형을 쓰면 늘어진다.
⛔ 서술형 본문 중간에 개조식 문장을 하나만 끼우지 말 것. 절 단위로 일관되게.
⛔ 입에서 안 나오는 말을 쓰지 말 것 — 「세웠다」「붙이나」「뭐가 인가」는 그래서 폐기했다.
ⓘ 근거 = `fitsel/.claude/skills/ux-writing` · `proposal-korean`(K01~K33).
  proposal-korean 의 K30 은 「읽는 본문」에만 적용된다(2026-09-22 에 그 구분을 추가했다).

## 레이아웃

- 첫 화면 54rem · 상세 문서 48rem · 본문 40em.
- **중앙 정렬 블록을 두지 않는다.** 전부 왼쪽 정렬.
- 카드를 쓰지 않는다. 구분은 1px 선과 종이 밝기 차이로만.

## ⭐ 결정 레일 — 이 사이트의 서명 장치

케이스의 「세 옵션, 둘 기각, 하나 채택」을 카드 그리드가 아니라 **본문을 관통하는 선의 분기**로 그린다.

```
├──┐  A. 전체 기업에 AI 검색을 돌린다
│  │  가장 단순하지만 60만 건마다 호출해야 한다.
│  ╵  ── 기각 · 원가              ← 레일이 물리적으로 끊긴다
├──┐  B. …
│  ╵  ── 기각 · 문제 회피
└──┐  C. 뉴스를 먼저 수집한다      ← 채택. 종이가 한 단 밝아지고 레일이 이어진다
   │
```

1. 기각된 갈래는 `::after` 로 끊긴다. 채택된 갈래만 아래로 흐른다.
2. 취소선·빨간 X 를 쓰지 않는다. 기각은 `opacity: .78` + `ink-muted`.
3. **판정 한 줄만 주홍.** 기각 판정도 주홍이다 — 「판정만 색을 쓴다」가 원칙이라
   기각을 회색으로 하면 판정이 죽는다(2026-09-22 실측).
4. 순수 CSS. JS 0, 스크롤 하이재킹 0.

## 모션

- 게이트 통과와 섹션 헤더 진입에만. **본문 모션 0.**
- 스크롤을 가로채지 않는다. `prefers-reduced-motion` 을 존중한다.

## 하지 말 것

- 3단 피처 그리드 · 아이콘 원 · 중첩 카드 · 카드 좌측 컬러 보더
- 보라·인디고 그라데이션 · 블롭 · 라디얼 할로 · 글로우
- 히어로 밑 큰 숫자 3개 (NDA 로 숫자가 거의 없다. 그 자리는 **판정 문장**이 먹는다)
- 헤딩 위 kicker · 6rem 초과 디스플레이 타입
- 스톡 일러스트 — Fitsel 제안서 표지 이미지가 여기 해당한다. 쓰지 말 것

## 결정 기록

| 날짜 | 결정 | 근거 |
|---|---|---|
| 2026-09-22 | 구조를 질문 목록으로 | 「소개→케이스→경력」이 교과서 공식이라 뻔했다 |
| 2026-09-22 | 제목 「물어야 했던 것들」 | 「다시」는 반복이지 변경이 아니다. 질문을 또 한 게 아니라 바꾼 것 |
| 2026-09-22 | 스케일 6배로 확대 | 한 뼘 안에 있어 밋밋했다 |
| 2026-09-22 | 기각 판정도 주홍 | 회색으로 하니 「판정만 색」 원칙이 무너졌다 |
| 2026-09-22 | 크림 대신 중성 회백 | 크림+세리프+테라코타 클리셰 회피 |
| 2026-09-22 | AI 목업 대신 실물 HTML | 한국어 조판과 1px 레일이 핵심이라 그림으로는 부정확 |
| 2026-09-22 | 지식그래프 / 그래프 DB 구분 | 만든 것과 필요 없던 도구는 다른 말이다 |
| 2026-09-22 | 질문을 「~까?」로 통일 | 종결이 제각각이고 입에서 안 나왔다 |
| 2026-09-22 | 「만든 것」 절을 개조식으로 | 한국 제안서 표준이 개조식인데 전부 서술형이었다 |

## 미해결

- 실물 이미지가 없다. Fitsel 화면을 넣기로 했으나 로그인 세션이 끊겨 미착수.
- 위험 3개 중 둘이 미구현 — 게이트를 히어로로 · Neo4j 글의 쿼리 버튼.
